#include<iostream>
using namespace std;
// const arguments
int main()
{
    int strlen(const char *p);
    int length(const string &s);
    return 0;
}


// qualifier const tells that the function should not modify that argument
// this type of declaration is only significant only when we pass argument by reference or pointers